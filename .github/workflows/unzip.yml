name: Unzip
on: [push]
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: unzip *.zip
      - run: rm *.zip
      - uses: stefanzweifel/git-auto-commit-action@v5
